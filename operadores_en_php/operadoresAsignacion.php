<?php

    $a = 30;
    $a = $a + 5 / 2 * 10;
    // Esto sería como decir $a = $a + 10
    $a += 10;
    // Esto sería como decir $a = $a / 10
    $a /= 2;
    // y etc...
    $a *= 1.5;

    print $a;